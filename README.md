# Caprover + Node + Typescript Hello, World! with Healthcheck

You need a valid caprover deployment somewhere and to have the caprover cli installed and configured.

```caprover deploy```

is all you need to do then.
